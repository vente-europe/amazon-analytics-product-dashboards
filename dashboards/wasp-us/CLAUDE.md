# WASP (US) — published copy

> This folder is a **published copy**, not the source of truth.

- **Source of truth:** `02-Projects/Dashboards/WASP - US/` (legacy Dashboards folder, own git repo `vente-europe/wasp-trap-US`, own CLAUDE.md with full tab documentation).
- `index.html` here is a verbatim copy of the source `index.html` — fully self-contained (no fetches), loaded by the hub via the standalone iframe fast-path.
- **To update:** edit in the source folder, then `cp` the file here and push both repos (`git push origin master:main` in each).
- 8 tabs: Main Segments / Lure / Electric / Reviews / VOC Category / VOC Spectracide Trap / VOC Trap Refill / POE Review Insights. (The older separate `WASP-VOC` hub dashboard was removed 2026-07-15 as superseded by this one; recoverable from git history.)
